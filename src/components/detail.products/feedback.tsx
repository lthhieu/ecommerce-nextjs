'use client'

import Box from "@mui/material/Box";

interface IProps {
}
const Feedback = (props: IProps) => {
    const { } = props;
    return (
        <Box>
            feedback
        </Box>
    )
}
export default Feedback